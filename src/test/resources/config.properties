# Application Configuration
app.base.url=https://www.saucedemo.com/
app.browser=chrome

# Test Data
test.username=standard_user
test.password=secret_sauce

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=10
page.load.timeout=30

# Browser Configuration
browser.maximize=true
browser.headless=false
browser.disable.notifications=true

# Test Environment
env=production
log.level=INFO

